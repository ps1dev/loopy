{
  "name": "loop-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based audio loop point editor with WAV smpl chunk support",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && vitest run",
    "test:unit": "vitest run --exclude '**/e2e.spec.ts'",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^2.1.0"
  }
}
